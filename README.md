# Telegram + Ollama

Телеграм-бот для чата с локальной моделью через Ollama.

## Запуск

1. Скопируйте `config.py.example` → `config.py` и `.env.example` → `.env`, заполните под себя.
2. Установите зависимости: `pip install -r requirements.txt`
