import os
from dotenv import load_dotenv

load_dotenv()

TELEGRAM_BOT_TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"
PROXY_URL = "socks5://YOUR_PROXY_IP:YOUR_PROXY_PORT"
ALLOWED_USER_IDS = ["YOUR_USER_ID"]
OLLAMA_HOST = os.getenv("OLLAMA_HOST")

INCLUDE_THINKING = False

DEFAULT_MODEL = "llama3.2"

# Корень для инструментов, если проект не выбран (абсолютный путь). По умолчанию — каталог этого файла.
TOOLS_ROOT = os.getenv("TOOLS_ROOT") or os.path.dirname(os.path.abspath(__file__))

# Имена проектов и абсолютные пути к их папкам. После /set_project <имя> все инструменты работают внутри этой папки.
PROJECTS: dict[str, str] = {
    # "my_app": r"C:\path\to\my_app",
    # "other": "/home/user/projects/other",
}

